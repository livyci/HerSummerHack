# Anthropic API key — used by the Smart Store frontend (Claude AI features).
# Get one from https://console.anthropic.com/  (used in-browser via dangerouslyAllowBrowser; demo only).
VITE_ANTHROPIC_API_KEY=your_key_here

# Optional: only needed if pairing with the Django backend.
# Leave empty in dev — Vite proxies /api to the local Django server.
# In production (Vercel), set to your Railway backend URL, e.g.:
# VITE_API_URL=https://your-app.up.railway.app
VITE_API_URL=
